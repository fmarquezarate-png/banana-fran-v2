[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — necesario para que React Router funcione con URLs directas
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
